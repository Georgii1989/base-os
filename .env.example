# TipJar through router = tips mint tip-profile soulbound (see README).
NEXT_PUBLIC_TIPJAR_ADDRESS=0xDd1090aFba3117953B892A6390B18abe5A979894
NEXT_PUBLIC_BASE_BUILDER_CODE=bc_59omft8w
NEXT_PUBLIC_APP_URL=https://app-base-os.vercel.app
NEXT_PUBLIC_SBT_ADDRESS=0x3736b7A4fC567192AA359CaDd8407786C556F729
NEXT_PUBLIC_SBT_FROM_BLOCK=45587033
# Optional: OS Supporter commemorative NFT (mint card on Tips tab)
NEXT_PUBLIC_OS_SUPPORTER_NFT_ADDRESS=0x7857051d974c37Cb9Ebe0c17DFdf13356619b498
# Tip profile getLogs: start from this Base block (deployment of TipJar/router) to avoid huge RPC windows.
NEXT_PUBLIC_TIP_PROFILE_FROM_BLOCK=45000000

# Token launcher — TokenFactory on Base (npm run contract:deploy:factory)
NEXT_PUBLIC_TOKEN_FACTORY_ADDRESS=0x4B7057Db4AE4914602faF5718E067BBe5920ff62

# Game tab — Grid646 1v1 (npm run contract:deploy:grid646)
NEXT_PUBLIC_GRID646_ADDRESS=0xE18cAe98DeF2189a2B57F8052B049247773b894f

# Battleship tab — 10×10 naval 1v1 (npm run contract:deploy:battleship10)
NEXT_PUBLIC_BATTLESHIP10_ADDRESS=0x73ee519B7231a57dADB95FBF46B072C388dC25d3
NEXT_PUBLIC_BATTLESHIP10_MASK_PLACEMENT=1

# Optional: CDP Paymaster for gasless tip + casual game in Base App (server-only key)
# CDP_API_KEY=
# CDP_PAYMASTER_RPC_URL=

# x402 paid API (Base mainnet) — CDP facilitator verify/settle + USDC payout wallet
# CDP_API_KEY_ID=
# CDP_API_KEY_SECRET=
# X402_PAY_TO_ADDRESS=0xYourBaseMainnetWallet
# X402_SCORE_PRICE=$0.001

# Optional: persistent Base Verify claims (Vercel KV / Upstash Redis REST)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Base Verify tab — live API (server-only; see README)
# BASE_VERIFY_API_URL=https://verify.base.dev/v1
# BASE_VERIFY_SECRET_KEY=

# Swap tab — 0x aggregator quotes (https://docs.0x.org — free API key)
ZEROX_API_KEY=

# Onchain score + watchlist use Blockscout (Base) by default — no key required.
# Optional: BLOCKSCOUT_API_KEY (https://base.blockscout.com) for higher rate limits.
# Optional: ETHERSCAN_API_KEY only if you have a paid plan with Base (chainid 8453) coverage.
BLOCKSCOUT_API_KEY=
BASESCAN_API_KEY=
ETHERSCAN_API_KEY=

